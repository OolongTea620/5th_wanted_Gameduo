const { redisCli } = require("../cache");
const error = require("../middlewares/errorConstructor");

const getRankerList = async (userId) => {
  // userLink로 배열
  // userId인 raidKey의 배열가져오기
};
module.exports = {};
